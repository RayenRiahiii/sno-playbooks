---
workflow_preinstallation_statuses:
  - pending-for-input
  - insufficient
  - ready

workflow_active_installation_statuses:
  - preparing-for-installation
  - installing
  - installing-pending-user-action
  - finalizing

workflow_failed_statuses:
  - error
  - cancelled

# Must be explicitly enabled for a recoverable finalization timeout.
failed_cluster_recovery_approved: false
